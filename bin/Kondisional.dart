main(List<String> args) {
  var number = -5;
  var output;

  output = (number > 0) ? 'Positif' : 'Negatif';
  // if (number > 0) {
  //   output = 'Positif';
  // } else if (number < 0) {
  //   output = 'Negatif';
  // } else {
  //   output = 'Nol';
  // }
  print('$number adalah $output');
}
